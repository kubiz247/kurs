<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="kantor.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <HorizontalStackLayout>
                <Button x:Name="CounterBtn" Text="Button" SemanticProperties.Hint="Counts the number of times you click" Clicked="OnCounterClicked" HorizontalOptions="Fill" />
                <Label>Waluta 1: </Label>
                <Entry x:Name="fe"></Entry>
                <Label>Waluta 2: </Label>
                <Entry x:Name="se"></Entry>
                <Label>kwota do przelcizania: </Label>
                <Entry Keyboard="Numeric" x:Name="pr"></Entry>
                <Label x:Name="wy" Text="Wynik"></Label>
            </HorizontalStackLayout>
            
        </VerticalStackLayout>

    </ScrollView>

</ContentPage>
